# 프로모션 정책

프로모션 P 는 카테고리 C 에만 적용 가능하다. 다른 카테고리에 속한 상품에는 프로모션 P 가 적용되지 않으며, 이는 시스템 검증 단계에서 강제된다.

프로모션 P 의 활성 기간은 별도 설정으로 관리되며, 활성 기간이 아닐 때는 어떤 쿠폰도 적용되지 않는다. 본 문서의 시점에서는 프로모션 P 가 활성 상태로 가정한다.

프로모션 Q 는 카테고리 D 에만 적용 가능하다. 카테고리 C 와 D 는 서로 배타적이다.
