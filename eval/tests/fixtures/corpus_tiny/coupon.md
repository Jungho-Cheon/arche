# 쿠폰 정책

쿠폰 X 는 프로모션 P 에 속한다. 쿠폰은 프로모션의 자식 객체로 관리되며, 프로모션이 비활성화되면 자동으로 함께 비활성화된다.

쿠폰 X 의 별칭은 "X 쿠폰", "할인 쿠폰 X" 가 있다. 동일 쿠폰을 가리키는 표현이며 시스템 내부적으로는 모두 같은 ID 를 가진다.

쿠폰 Y 는 프로모션 Q 에 속한다. 프로모션 Q 는 카테고리 D 에만 적용되며, 본 문서에서는 자세히 다루지 않는다.
