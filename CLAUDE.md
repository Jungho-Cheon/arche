# Working with this repo

## 이 저장소는 어디서 시작하나

이 저장소는 **2026-06-15 PRD 재정립으로 새로 시작된 Opentology MVP** 의 단일 진실의 원천이다. 이전 가설 기반의 작업 (FE, 인증, 멀티테넌트, 채팅 등) 은 별도 저장소 `legacy-opentology` 에 보존되어 있으며, *본 저장소의 의사결정과 무관* 하다. 옛 작업의 산출물을 재참조해야 하는 시나리오를 제외하고는 *옛 저장소를 들여다보지 않는다* — 환각 방지의 핵심.

## 시작 시 진입점 (순서)

새 세션 (사용자 본인 또는 서브에이전트) 은 다음 순서로 컨텍스트를 잡는다.

1. **[`docs/prd/1_mvp.md`](./docs/prd/1_mvp.md)** — MVP 사양. 무엇을 만들고 무엇을 검증하는가.
2. **[`docs/adr/README.md`](./docs/adr/README.md)** — ADR 인덱스. 0001 부터 0006 까지 순서대로 읽으면 한 묶음.
3. **[STATUS.md](./STATUS.md)** — 현재 진행 상태. 구현 계획서 작성 전에는 placeholder.
4. **(구현 계획서 추가 시)** `docs/superpowers/specs/` — 구현 단계별 작업 계획.

이 4 단계 외의 자료에 *암묵적으로 의존하지 않는다* . 옛 가설의 흔적 (옛 ADR 번호, 옛 도메인 모델 이름, 옛 컴포넌트 등) 이 컨텍스트에 등장하면 *환각이거나 옛 저장소 참조* 다.

## 코드 파악 시

코드 의존성 / 호출 관계 / 영향 범위를 파악할 때는 `grep` / `find` / 직접 파일 읽기로 한다. ADR / PRD / 코드 사이 교차 질문은 PRD 와 ADR 의 명시적 본문 + 코드의 import / call 관계를 직접 추적해 답한다.

## 세션 역할 모드

이 저장소는 **오케스트레이터 모드** 와 **워커 모드** 두 가지 방식으로 운영한다. 세션 시작 시 어느 모드인지 먼저 확정한다.

- **워커 모드** — 사용자가 부모 (orchestrator) 로부터 디스패치된 단일 이슈를 진행. 워크트리는 이미 격리되어 있고, 코드를 직접 수정·테스트·커밋·PR 생성까지 수행한다.
- **오케스트레이터 모드** — 사용자가 여러 이슈를 분배·조율. 코드는 직접 만지지 않는다.

기본값은 워커 모드. 사용자가 "오케스트레이터로 동작" 또는 "작업 분배" 를 명시하면 오케스트레이터 모드로 전환한다.

## 작업이 끝났을 때 (워커 모드)

- 작업에 대한 PR 을 생성한다. PR 본문은 `create-pr` 스킬의 템플릿을 활용한다.
- 커밋 메시지 또는 PR 본문에 `Closes #N` 을 포함해 머지 시 Issue 가 자동 종료되도록 한다.
- 사용자가 명시적으로 요청하지 않은 한 ADR 은 자동으로 갱신하지 않는다 (단 ADR 작성이 본 작업의 일부인 경우는 제외).

## 공개 아티팩트 접근성 규칙

GitHub Issue / Milestone / `docs/adr/` 본문을 **새로 작성하거나 본문을 크게 손볼 때** 는 비전공자나 처음 들어온 제3자도 본문만 읽고 의도를 따라갈 수 있게 쓴다. 약어 (`ADR`, `MCP`, `RAG`) 와 내부 용어는 처음 등장할 때 한 줄로 풀어 설명하고, "지난 ADR 그대로" 같은 사내 맥락에 기대지 않고 한 줄 요약을 동반한다. 이슈 본문은 "배경 / 무엇을 할 것인가 / 완료 조건" 세 블록을 최소 골격으로, 마일스톤 설명은 "이게 끝나면 무엇이 가능해지는가" 를 한 단락으로. 자세한 가이드는 메모리 `feedback-artifact-accessibility` 참조.

## 작성 톤

ADR / PRD / 이슈 등 공식 문서에 다음 부류 표현을 쓰지 않는다 (자세한 정책은 메모리 `feedback-writing-tone`).

1. **사내·영어 직역 jargon** — "도그푸딩" 등. 대안: "직접 사용", "검증 도메인" 등.
2. **구어체 동사** — "박는다 / 박혀 있다" 등. 대안: "명시한다 / 기록한다 / 정한다 / 확정한다".

## 저장소 레이아웃

현재 본 저장소는 *문서만* 담는다. 코드 (`apps/api` 등) 는 구현 계획서 작성 후 추가된다.

- `docs/prd` — 제품 요구사항. MVP 사양은 `docs/prd/1_mvp.md`.
- `docs/adr` — 의사결정 기록. 새 ADR 은 다음 번호로. 읽기 순서 가이드는 `docs/adr/README.md`.
- `docs/superpowers/specs` — 구현 계획 spec (구현 계획서 작성 후 채워짐).
- `STATUS.md` — 현재 진행 상태.
- `README.md` — 저장소 첫인상.

## 관련 저장소

- **`legacy-opentology`** (별도 저장소) — 2026-06-15 PRD 재정립 이전의 모든 작업 (옛 ADR 26 개, 옛 spec 4 개, 옛 `apps/`, 옛 STATUS) 이 보존되어 있다. 이 저장소는 *능동 작업 대상이 아니며* 새 가설과 무관하다. 재참조가 명시적으로 필요한 경우에만 들여다본다.
